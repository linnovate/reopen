apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.4.0 (c7964e7)
  creationTimestamp: null
  labels:
    io.kompose.service: root-app
  name: root-app
spec:
  replicas: {{ .Values.root_app.deployment.replicas }} 
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: root-app
    spec:
      containers:
      - image: "{{ .Values.image.registry }}/{{ .Values.root_app.deployment.image.name }}:{{ .Values.root_app.deployment.image.tag }}"
        imagePullPolicy: Always
        name: root-app
        ports:
        - containerPort: {{ .Values.root_app.service.default.targetPort }} 
        resources: {}
        volumeMounts:
          - name: root-uploads
            mountPath: /usr/src/app/files
            readOnly: false
      volumes:
      - name: root-uploads
        hostPath:
          path: "/data/uploads/linnovate/root/{{ .Release.Namespace }}"
      restartPolicy: Always
status: {}
