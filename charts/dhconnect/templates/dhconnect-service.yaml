apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.4.0 (c7964e7)
  creationTimestamp: null
  labels:
    io.kompose.service: dhconnect
  name: dhconnect
spec:
  type: ClusterIP
  ports:
  - name: "{{ .Values.dhconnect.service.default.name }}"
    port: {{ .Values.dhconnect.service.default.port }} 
    targetPort: {{ .Values.dhconnect.service.default.targetPort }} 
  selector:
    io.kompose.service: dhconnect
status:
  loadBalancer: {}
